version: '3.8'

services:
  akatsuchizaki-3d:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./:/app
    environment:
      - NODE_ENV=development
    container_name: akatsuchizaki-3d-viewer
