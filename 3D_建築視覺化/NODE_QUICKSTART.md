# 🚀 Node.js 快速啟動指南

**最快的方式** ⚡ (3 命令 = 30秒)

---

## 為什麼用 Node.js?

```
✅ 簡單快速
✅ 無需 Docker
✅ 完全解決 CORS 問題
✅ 支援 API 端點
✅ 實時服務
```

---

## 📋 前置要求

### 檢查 Node.js 是否已安裝

打開 PowerShell 或 CMD，執行:

```bash
node --version
npm --version
```

**預期輸出**:
```
v18.0.0  (或更新版本)
8.0.0    (或更新版本)
```

### 如果未安裝

下載並安裝: https://nodejs.org/

建議下載 **LTS (長期支援)** 版本

---

## ⚡ 快速啟動 (3 步)

### 步驟 1: 進入目錄

```bash
cd C:\Users\thc1006\Desktop\dev\easterlin-hsinchu\3D_建築視覺化
```

### 步驟 2: 安裝依賴

```bash
npm install
```

**預期輸出**:
```
added XX packages in Xs
```

### 步驟 3: 啟動伺服器

```bash
npm start
```

**預期輸出**:
```
╔════════════════════════════════════════════════════════════╗
║  ✅ 赤土崎多功能館 3D 可視化伺服器已啟動                 ║
╠════════════════════════════════════════════════════════════╣
║  🌐 主頁: http://localhost:8080                           ║
║  📍 完全獨立版: http://localhost:8080/standalone          ║
║  📍 原始版: http://localhost:8080/original                ║
╚════════════════════════════════════════════════════════════╝
```

### 步驟 4: 打開瀏覽器

```
http://localhost:8080
```

🎉 **完成！** 你應該看到 3D 建築模型了

---

## 📚 可用的 URL

| URL | 說明 |
|-----|------|
| http://localhost:8080 | 主頁 (完全獨立版) |
| http://localhost:8080/standalone | 完全獨立版 |
| http://localhost:8080/original | 原始版 (需網路) |
| http://localhost:8080/api/floors | 獲取樓層數據 |
| http://localhost:8080/api/building | 獲取建築規格 |
| http://localhost:8080/health | 健康檢查 |

---

## 🔧 常用命令

### 啟動伺服器

```bash
npm start
```

### 停止伺服器

```bash
Ctrl+C
```

### 重新安裝依賴

```bash
npm install --no-save
```

### 檢查版本

```bash
node --version
npm --version
npm list
```

---

## 🐛 故障排除

### 問題 1: 找不到 npm 命令

**原因**: Node.js 未正確安裝

**解決**:
```bash
# 重新安裝 Node.js
# https://nodejs.org/

# 驗證安裝
node --version
npm --version
```

### 問題 2: 埠口 8080 已被佔用

**原因**: 另一個程式在使用埠口 8080

**解決方案 A**: 停止另一個程式

```bash
# 查找佔用埠口的程式
netstat -ano | findstr :8080

# 停止該程式
taskkill /PID <PID> /F
```

**解決方案 B**: 使用另一個埠口

編輯 `server.js`，第 13 行:

```javascript
// 改為:
const PORT = 3000;  // 或 8000, 9000 等
```

然後重啟伺服器

### 問題 3: npm install 失敗

**原因**: 網路問題或 npm 暫存問題

**解決**:
```bash
# 清除 npm 暫存
npm cache clean --force

# 重新安裝
npm install
```

### 問題 4: 瀏覽器打開時顯示 Cannot GET /

**原因**: 路由配置問題

**解決**:
```bash
# 檢查 server.js 是否正確
# 重啟伺服器
npm start

# 或訪問
http://localhost:8080/standalone
```

---

## 🚀 進階用法

### 使用 Nodemon 自動重載 (開發模式)

```bash
npm install -g nodemon
nodemon server.js
```

檔案變化時會自動重啟伺服器

### 更改預設埠口

編輯 `server.js`:

```javascript
const PORT = 3000;  // 改為你想要的埠口
```

### 添加新的 API 端點

編輯 `server.js`，在「路由」部分添加:

```javascript
app.get('/api/custom', (req, res) => {
    res.json({ message: 'Your custom response' });
});
```

重啟伺服器，訪問 `http://localhost:8080/api/custom`

---

## 📊 效能提示

### 檢查 CPU/記憶體 使用

```bash
# Windows
tasklist | findstr node

# 或使用系統工作管理員
taskmgr
```

### 正常的資源使用

```
記憶體: 30-50 MB
CPU: <5% (閒置時)
```

---

## 🎯 推薦流程

```
1️⃣ 安裝 Node.js
   ↓
2️⃣ npm install
   ↓
3️⃣ npm start
   ↓
4️⃣ 打開 http://localhost:8080
   ↓
5️⃣ 享受 3D 建築體驗！
```

---

## 📞 如果還有問題

**檢查清單**:
- [ ] Node.js 已安裝 (node --version)
- [ ] npm 已安裝 (npm --version)
- [ ] 進入了正確的目錄 (pwd)
- [ ] npm install 成功執行
- [ ] npm start 顯示「已啟動」訊息
- [ ] 瀏覽器能訪問 http://localhost:8080

**如果還是無法工作**:
1. 複製完整錯誤信息
2. 執行 `npm --version` 的輸出
3. 執行 `node --version` 的輸出
4. 發送給我診斷

---

## ✨ 一鍵啟動 (批次指令碼)

### Windows 批次檔案 (start.bat)

創建檔案 `start.bat`:

```batch
@echo off
echo 啟動赤土崎 3D 可視化伺服器...
node server.js
pause
```

雙擊 `start.bat` 啟動伺服器

### PowerShell 指令碼 (start.ps1)

創建檔案 `start.ps1`:

```powershell
Write-Host "啟動赤土崎 3D 可視化伺服器..." -ForegroundColor Green
node server.js
```

執行:
```bash
powershell -ExecutionPolicy Bypass -File start.ps1
```

---

**現在就啟動伺服器，享受完整的 3D 建築可視化體驗吧！** 🎉

```bash
cd C:\Users\thc1006\Desktop\dev\easterlin-hsinchu\3D_建築視覺化
npm install
npm start
```

然後在瀏覽器打開: **http://localhost:8080**

祝你使用愉快! ✨
